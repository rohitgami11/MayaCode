import React from 'react';
import { View } from 'react-native';

// This screen doesn't render anything - the tab button opens the sidebar directly
export default function MenuScreen() {
  return <View style={{ flex: 1 }} />;
}

